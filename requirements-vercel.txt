# Minimal requirements for Vercel deployment
# Note: sentence-transformers and transformers are NOT included
# Embeddings will be done via Supabase or API calls

# API Framework
flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.1

# Utilities
python-dotenv==1.0.0
pydantic>=2.6.3,<3.0.0
pydantic-settings==2.1.0

# File handling (synchronous for Flask)

# HTTP requests for LLM API
requests==2.31.0
huggingface_hub>=0.20.0

# Database and Authentication
sqlalchemy==1.4.46
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
email-validator==2.1.0  # Required for pydantic EmailStr

# Supabase/PostgreSQL support
psycopg2-binary==2.9.9
pgvector==0.2.3

# Document Processing (lightweight)
PyPDF2==3.0.1
python-docx==1.1.0
openpyxl==3.1.2

# NOTE: ChromaDB and sentence-transformers are NOT included
# Embeddings should be done via Supabase or external API
# If you need local embeddings, use a separate service
